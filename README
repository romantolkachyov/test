Install and run
===============

$ pip install -r requirements-dev.txt

$ nodeenv -p

$ npm install -g gulp

$ gulp

$ ./manage.py syncdb

$ ./manage.py makemigrations flexmodel

$ ./manage.py runserver

Running tests
=============

$ ./manage.py test --settings=project.settings_test

because tests use their own db config `test_models.yaml`
